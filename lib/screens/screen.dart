import 'package:flutter/material.dart';


class ActualScreen extends StatelessWidget {
  const ActualScreen({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}